---
all:
  hosts:
    host-lt:
      ansible_host: localhost
    host-vc:
      ansible_host: localhost
    host-cl:
      ansible_host: localhost
  vars:
    ansible_connection: ssh
    ansible_user: arsen
    ansible_python_interpreter: /usr/bin/python3
clickhouse:
  hosts:
    host-cl:
vector:
  hosts:
    host-vc:
lighthouse:
  hosts:
    host-lt:
